# Testing
pytest>=7.0

# Range/equity engine
numpy>=1.20
scikit-learn>=1.8
lightgbm>=4.6

# Hand evaluation — C-accelerated 7-card eval. HandEvaluator.eval_int falls
# back to the pure-Python evaluator if eval7 isn't installed, but the test
# suite expects eval7 to be available.
eval7>=0.1.10

# WSS MITM proxy (pf_intercept)
websockets>=14.0
cryptography>=42.0
certifi>=2024.0

# Protobuf — install after compiling protos with pf_intercept/gen_pb2.sh
protobuf>=4.0
